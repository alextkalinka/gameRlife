#' seed_random
#' 
#' Creates a random seed for `gameRlife::game_of_life` for a given board size and proportion of living cells.
#' 
#' @param board_size An integer specifying the size of the 2D array.
#' @param proportion_living A real number in range (0,1).
#' 
#' @return A matrix with `percent_living`*`board_size^2` rows and 2 columns providing the co-ordinates of the random seed living cells.
#' @author Alex Kalinka, \email{alex.t.kalinka@@gmail.com}
#' @export
seed_random <- function(board_size){
  
}
